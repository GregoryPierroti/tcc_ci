| ID da mutação | Rodada | Prática de engenharia de software avaliada | Objetivo técnico do controle | Ferramenta/check | Falha injetada | Detector esperado | Primeiro detector observado | Classificação do resultado | Interpretação/limite |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `PY-001` | v1 | Análise estática de código | Impedir que erro sintático alcance a execução | Ruff no gate de lint | Parêntese não fechado no retorno de `_criar_chave_nome` | Ruff | Ruff format | Detecção precedente | O formatador fez parse e bloqueou antes do lint; isso não demonstra correção funcional. |
| `PY-002` | v1 | Teste automatizado de regressão | Preservar o comportamento determinístico da normalização | pytest | Troca de `.upper()` por `.lower()` | pytest | pytest | Detecção direta | A evidência vale para os casos de normalização exercitados, não para toda entrada possível. |
| `PY-003` | v1 | Teste automatizado de regressão | Preservar filtro e comportamento do join | pytest | Filtro de CNPJ válido invertido de `!= '0'` para `== '0'` | pytest | pytest | Detecção direta | O teste revelou join vazio e ausência de publicação; a cobertura limita-se ao cenário catalogado. |
| `PY-004` | v1 | Teste de contrato sobre transformação | Detectar quebra da interface de colunas usada pelo join | pytest | Uso da coluna inexistente `Nome` no segundo join | pytest | pytest | Detecção direta | O `KeyError` comprova a quebra daquela interface, não a validade geral do modelo de dados. |
| `PY-005` | v1 | Gerenciamento reprodutível de dependências | Garantir que o ambiente seja reconstruído do lockfile íntegro | `uv sync --frozen` na preparação do check | Entrada da wheel Linux do pandas corrompida no `uv.lock` | Validação do lockfile pelo uv | Validação do lockfile pelo uv | Detecção direta | A construção parou antes de Ruff; o resultado avalia resolução de dependências, não execução do pipeline. |
| `SP-001` | v1 | Teste unitário com runtime Spark | Preservar transformação determinística executada pelo Spark | pytest com Spark | Troca de `F.upper` por `F.lower` | pytest | pytest | Detecção direta | Exercita a normalização definida no teste, não todas as combinações do runtime Spark. |
| `SP-002` | v1 | Teste unitário com runtime Spark | Detectar referência inválida a coluna em transformação | pytest com Spark | Uso de `Nome_inexistente` na criação da chave | pytest | pytest | Detecção direta | O Spark revelou coluna não resolvida; schemas dinâmicos não exercitados continuam fora da evidência. |
| `SP-003` | v1 | Validação end-to-end de schema | Verificar o efeito do schema na execução integral | Execução integral e validação externa | Tipo de `CNPJ` alterado de `StringType` para `DoubleType` | Execução integral e validação de dados, fora da CI basal | Nenhum na CI | Falso negativo | A CI passou; a confirmação externa produziu 0 linhas em vez de 154. Esse controle não integrava o workflow basal. |
| `SP-004` | v1 | Teste de regressão de cardinalidade | Detectar alteração indevida do volume produzido por join | Execução integral e validação externa | Join bancos–reclamações alterado de `inner` para `left` | Execução integral e validação de contagem, fora da CI basal | Nenhum na CI | Falso negativo | A CI passou; a confirmação externa produziu 1.589 linhas em vez de 154. É evidência histórica de uma lacuna da v1. |
| `SP-005` | v1 | Gerenciamento reprodutível de dependências | Garantir resolução íntegra do ambiente PySpark | `uv sync --frozen` na preparação do check | URL de `sdist` do PySpark corrompida no `uv.lock` | Validação do lockfile pelo uv | Validação do lockfile pelo uv | Detecção direta | A construção parou antes de Ruff; não houve avaliação das etapas posteriores. |
| `DBT-001` | v1 | Análise estática de SQL templado | Bloquear construção SQL incompatível com as regras versionadas | SQLFluff com templater dbt | Vírgula removida entre colunas do CTE `bancos` | dbt parse | SQLFluff AL02 | Detecção precedente | A remoção formou alias implícito, não SQL inválido; parse e compile passaram. O lint bloqueou a causa antes do detector previsto. |
| `DBT-002` | v1 | Validação de grafo de dependências | Garantir que referências entre modelos sejam resolvíveis | SQLFluff com templater dbt e dbt parse | `ref('mod_bancos')` trocada por `ref('modelo_inexistente')` | dbt parse | SQLFluff com templater dbt | Detecção precedente | O templater precisou resolver o grafo durante o lint e bloqueou antes do parse explícito. |
| `DBT-003` | v1 | Teste de integridade de dados | Impedir nulidade em chave declarada como obrigatória | dbt build + teste `not_null` | Projeção de `null::text` como `cnpj` | Teste `not_null` | `not_null_mod_final_cnpj` | Detecção direta | Lint, parse e compile passaram; o achado depende da restrição declarada e pertence ao catálogo histórico da v1. |
| `DBT-004` | v1 | Teste de integridade de dados | Impedir duplicidade em chave declarada como única | dbt build + teste `unique` | Duplicação de `employer_sk` com `union all` | Teste `unique` | `unique_mod_empregados_employer_sk` | Detecção direta | O teste encontrou 39 duplicidades; a evidência não se generaliza a restrições não declaradas. |
| `DBT-005` | v1 | Teste de regressão de dados | Preservar cardinalidade de referência do modelo final | dbt build + teste singular `baseline_counts` | Join final alterado de `inner` para `left` | Teste singular `baseline_counts` | `baseline_counts` | Detecção direta | O teste revelou 327 linhas em vez de 1; esse critério de data quality não foi transposto para o recorte da v2. |
| `V2-PY-001` | v2 | Verificação automatizada de formatação | Manter representação mecânica canônica do código versionado | Ruff format | Remoção de linha em branco antes de classe local | Ruff format | Ruff format | Detecção direta | O check atuou como gate, mas formatação não prova correção funcional. |
| `V2-PY-002` | v2 | Análise estática de código | Detectar referência a identificador inexistente antes do runtime | Ruff F821 | Referência a `identificador_v2_inexistente` no processamento | Ruff F821 | Ruff F821 | Detecção direta | O gate bloqueou a referência estática; resolução exclusivamente dinâmica permanece fora do alcance. |
| `V2-PY-003` | v2 | Análise estática de tipos | Verificar compatibilidade entre orquestração e porta tipada | mypy | Texto atribuído à porta tipada de persistência | mypy | Ruff F401; mypy na repetição isolada | Detecção precedente; depois direta | A primeira forma deixou import ocioso e Ruff bloqueou antes; a repetição manteve o import em uso e isolou a detecção por mypy. |
| `V2-PY-004` | v2 | Verificação automatizada de documentação | Manter docstrings mecanicamente uniformes | pydocstringformatter | Espaço interno excedente em docstring | pydocstringformatter | pydocstringformatter | Detecção direta | O check foi bloqueante; conformidade de formato não garante que a documentação seja verdadeira. |
| `V2-PY-005` | v2 | Análise de complexidade ciclomática | Limitar o número de caminhos independentes por unidade de código | Radon | Ramificações acrescentadas até complexidade C (14) | Radon | Radon reportou C (14), sem bloquear | Falso negativo | A ferramenta estava no workflow, mas `radon cc . -n B -s` apenas reportava; presença sem saída não zero não constituiu gate. |
| `V2-PY-006` | v2 | Análise estática de código morto | Identificar unidade privada sem referência | Vulture | Função privada sem uso | Vulture | Vulture indicou confiança 60 localmente; nenhum bloqueio na CI | Falso negativo | O check era bloqueante apenas para achados no limiar 100; a mutação ficou abaixo da política configurada. |
| `V2-PY-007` | v2 | Teste de arquitetura | Impedir dependência invertida entre adaptador e pipeline | import-linter | `utils.s3_client` importando `pipeline.ingestao_raw` | import-linter | import-linter | Detecção direta | O contrato arquitetural declarado foi bloqueante; dependências não declaradas não são cobertas. |
| `V2-PY-008` | v2 | Teste unitário | Detectar regressão determinística na normalização | pytest | Normalização alterada de maiúsculas para minúsculas | pytest | pytest | Detecção direta | Três testes falharam; os casos unitários não representam todas as entradas de produção. |
| `V2-PY-009` | v2 | Teste baseado em propriedades | Verificar invariante de remoção de espaços para entradas variadas | pytest com Hypothesis | Remoção de `strip()`, preservando espaços externos | pytest com Hypothesis | pytest com Hypothesis | Detecção direta | Hypothesis encontrou o contraexemplo mínimo; entradas geradas não equivalem ao perfil de produção. |
| `V2-PY-010` | v2 | Gate de cobertura de testes | Exigir pelo menos 95% das linhas instrumentadas na suíte unitária | pytest-cov | Ramo técnico sem teste, reduzindo cobertura unitária para 94,66% | pytest-cov | Cobertura agregada pela CI, sem bloqueio | Falso negativo | A ferramenta e o limiar existiam, mas a CI agregou testes unitários e de integração; o gate mediu uma composição diferente da hipótese. |
| `V2-PY-011` | v2 | Teste de integração de adaptador | Verificar contrato técnico de publicação no PostgreSQL | pytest de integração | Publicação redirecionada ao schema inexistente `raw_v2_inexistente` | pytest | pytest | Detecção direta | O teste bloqueou `InvalidSchemaName`; não avaliou o conteúdo analítico publicado. |
| `V2-PY-012` | v2 | Teste de integração de serviço | Verificar contrato técnico entre cliente e armazenamento de objetos | pytest de integração com MinIO | Cliente apontado para bucket inexistente | pytest | pytest | Detecção direta | O teste bloqueou `NoSuchBucket`; encontrar o bucket ou objeto não valida seu conteúdo. |
| `V2-PY-013` | v2 | Teste de infraestrutura reproduzível | Verificar que o bootstrap provisiona a dependência exigida | Docker Compose + pytest de integração | Criação do bucket `etl-python` removida do `minio-init` | pytest | pytest | Detecção direta | O teste revelou bootstrap incompleto; isso não mede políticas da infraestrutura de produção. |
| `V2-PY-014` | v2 | Teste de idempotência | Garantir reexecução segura do bootstrap de recursos | pytest de integração | `ensure_base_structure` falha quando o bucket já existe | pytest | pip-audit `PYSEC-2026-3721`; nenhum na repetição | Falso positivo externo; depois falso negativo | A primeira CI ficou vermelha por vulnerabilidade real e independente. Com baseline segura, a CI passou porque a suíte executava apenas uma inicialização. |
| `V2-SP-001` | v2 | Análise estática de código | Detectar nome Python inexistente antes de inicializar o Spark | Ruff F821 | Referência a identificador inexistente no leitor | Ruff F821 | Ruff F821 | Detecção direta | O gate estático bloqueou a causa; não valida schemas de DataFrame resolvidos somente no runtime. |
| `V2-SP-002` | v2 | Análise estática de tipos | Verificar compatibilidade nas fronteiras Python tipáveis do pipeline Spark | mypy | Lista atribuída a `base_dir: str` | mypy | mypy | Detecção direta | O check bloqueou a fronteira Python; mypy não infere colunas internas de DataFrames Spark. |
| `V2-SP-003` | v2 | Teste unitário com runtime Spark | Detectar regressão de normalização em sessão isolada | pytest com Spark | `F.upper` trocado por `F.lower` | pytest | pytest | Detecção direta | Dois testes bloquearam a mutação; não há alegação sobre semântica da fonte de dados. |
| `V2-SP-004` | v2 | Teste baseado em propriedades | Verificar invariante de normalização para entradas variadas no Spark | pytest com Hypothesis | Remoção de `F.trim`, preservando espaços externos | pytest com Hypothesis | pytest | Detecção direta pela suíte, sem atribuição exclusiva | A mutação foi bloqueada, mas um teste determinístico também a cobria; o experimento não isola Hypothesis como único detector. |
| `V2-SP-005` | v2 | Gate de cobertura de testes | Exigir pelo menos 95% das linhas instrumentadas na orquestração | pytest-cov | Caminho técnico sem teste, reduzindo cobertura unitária para 94,58% | pytest-cov | pip-audit `PYSEC-2026-3721`; cobertura agregada sem bloqueio na repetição | Falso positivo externo; depois falso negativo | A primeira falha era independente. Na baseline segura, a ferramenta mediu cobertura, mas a composição remota não preservou o gate unitário. |
| `V2-SP-006` | v2 | Teste de integração de adaptador | Verificar disponibilidade da conexão Spark–JDBC | pytest de integração Spark–JDBC | Porta PostgreSQL alterada de 5432 para 5433 | pytest | pytest | Detecção direta | A conexão recusada foi bloqueada; indisponibilidade de rede não é defeito de conteúdo de dados. |
| `V2-SP-007` | v2 | Teste de integração de contrato | Verificar consistência entre relação publicada e relação lida via JDBC | pytest de integração Spark–JDBC | Leitura de tabela com nome diferente do publicado | pytest | Ruff format; pytest na repetição isolada | Detecção precedente; depois direta | A primeira forma violou formatação e foi bloqueada antes; a repetição isolou a falha de relação inexistente. |
| `V2-SP-008` | v2 | Teste de integração de persistência | Garantir que a entrega seja efetivamente publicada via JDBC | pytest de integração Spark–JDBC | Persistência JDBC removida após a criação da entrega | pytest de integração | pytest unitário de orquestração | Detecção precedente | O teste unitário viu a lista de publicações vazia antes da integração; isso mostra defesa em profundidade, não crédito ao detector esperado. |
| `V2-DBT-001` | v2 | Análise estática de SQL | Manter conformidade do SQL versionado sem alterar sua semântica | SQLFluff | Espaço duplicado antes de `*` no `select` final | SQLFluff | SQLFluff LT01 | Detecção direta | O gate bloqueou estilo; conformidade estilística não prova lógica SQL correta. |
| `V2-DBT-002` | v2 | Validação de grafo de dependências | Impedir referência ausente entre modelos dbt | dbt parse, precedido por SQLFluff com templater dbt | `ref` válida trocada por `ref('modelo_inexistente')` | dbt parse | SQLFluff com templater dbt | Detecção precedente | O templater precisou compilar o projeto no lint e bloqueou a mesma causa antes do parse explícito. Grafo válido não prova resultado SQL. |
| `V2-GOV-001` | v2 | SAST (*Static Application Security Testing*) | Detectar padrão de execução de comandos com risco de injeção | Bandit B602 | Auxiliar com `subprocess.run(..., shell=True)` | Bandit B602 | Bandit B602 | Detecção direta | A regra heurística bloqueou após os testes; um resultado limpo não provaria ausência de vulnerabilidades. |
| `V2-GOV-002` | v2 | *Secret scanning* | Impedir assinatura de segredo sintético no código versionado | Gitleaks | Token GitHub sintético e inválido, primeiro como constante e depois em comentário | Gitleaks | Bandit B105; nenhum na repetição isolada | Detecção precedente; depois falso negativo | Na primeira tentativa, outro controle bloqueou a mesma causa. Sem essa interferência, Gitleaks não bloqueou a assinatura específica; isso não se generaliza a todo segredo. |
| `V2-GOV-003` | v2 | Lint de configuração de pipeline | Validar sintaxe e expressões do workflow antes da execução remota | Actionlint | Chamada incompleta a `always()` em condição do GitHub Actions | Actionlint | Actionlint | Detecção direta | O gate bloqueou a expressão inválida; sintaxe válida não garante política de CI adequada. |
| `V2-GOV-004` | v2 | Lint de Dockerfile | Promover higiene e reprodutibilidade das camadas de imagem | Hadolint | `apt-get update` separado da instalação e da limpeza das listas | Hadolint | Hadolint DL3009 | Detecção direta | O gate bloqueou a camada inadequada; lint não substitui scanner de imagem nem teste em runtime. |
