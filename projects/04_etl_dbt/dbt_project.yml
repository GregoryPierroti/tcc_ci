name: my_project
version: 1.0
config-version: 2
profile: my_postgres_profile

model-paths: ["dbt/project/models"]
seed-paths:  ["dbt/project/seeds"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]
macro-paths: ["dbt/project/macros"]  